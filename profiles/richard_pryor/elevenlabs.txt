# ElevenLabs TTS configuration for the Richard Pryor profile.
# Format: key=value (one per line; comments start with #).

# Voice name: must match a voice in your ElevenLabs catalog. Pryor's voice was
# conversationally warm at baseline with sudden urgent surges and flexible
# character-voice work mid-bit. Closest prebuilt options:
#   Charlie — mid-range, articulate, natural cadence  (clean baseline with good
#              responsiveness for the snap shifts between warmth and urgency)
#   George  — warmer, deeper baseline                 (more gravitas, slightly
#              less agile for rapid character-voice transitions)
#
# Chosen: Charlie — the mid-range articulation better supports Pryor's
# character-voice flexibility; George is warmer but the snap shifts require
# the lighter, more responsive register Charlie provides.
voice=Charlie

# No IVC/PVC clone committed here. If you train a Pryor clone, add a sibling
# elevenlabs.local.txt (gitignored) with:
#   voice_id=<your_ivc_or_pvc_voice_id>
# That file overrides this one at runtime.

# Voice stability: LOW — Pryor's delivery spans from quiet confessional warmth
# to urgent burst and full character-voice mode. The stability must be low
# enough to carry all of those registers without flattening them.
stability=0.30

# Similarity boost: high to keep Charlie's identity intact through the wide
# stability swings and character-voice passages.
similarity_boost=0.73
