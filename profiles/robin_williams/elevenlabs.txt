# ElevenLabs TTS configuration for the Robin Williams profile.
# Format: key=value (one per line; comments start with #).

# Voice name: must match a voice in your ElevenLabs catalog. Williams' voice
# was fast, elastic, and capable of violent mid-sentence register shifts —
# a baseline with manic surge potential and dip-to-whisper range. Closest
# prebuilt options:
#   Liam   — younger, agile, light baseline         (fast cadence, responsive
#             to delivery cues; good for the rapid shifts and character voices)
#   Brian  — deep, resonant, wider range             (more gravitas; handles
#             the manic peaks but less agile on the ultra-fast riffs)
#
# Chosen: Liam — elasticity and agility beat raw depth here; Williams' defining
# trait is the speed and the mid-sentence pivot, which Liam's lighter frame
# handles better than Brian's grounded resonance.
voice=Liam

# No IVC/PVC clone committed here. If you train a Williams clone, add a sibling
# elevenlabs.local.txt (gitignored) with:
#   voice_id=<your_ivc_or_pvc_voice_id>
# That file overrides this one at runtime.

# Voice stability: LOW — Williams' delivery is defined by unpredictability.
# Low stability lets the voice accelerate into mania and drop into tenderness
# within a single breath, which is the core of the persona.
stability=0.28

# Similarity boost: high to preserve the voice identity through the widest
# possible delivery swings; without it, the manic peaks lose coherence.
similarity_boost=0.75
