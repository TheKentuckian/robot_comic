# ElevenLabs TTS configuration for the Bill Hicks profile.
# Format: key=value (one per line; comments start with #).

# Voice name: must match a voice in your ElevenLabs catalog. Hicks lands best
# on a deep, resonant baseline that can carry both the weary drawl and the
# ignited intensity. Alternatives in the prebuilt catalog:
#   Brian  — deep, resonant, comforting   (default — middle-aged American)
#   Adam   — dominant, firm                (more intensity, less warmth)
#   Callum — husky trickster               (grittier, smaller register)
#   Chris  — charming, down-to-earth       (more casual)
voice=Brian

# No PVC/IVC clone for Hicks — using a prebuilt voice. If you train one later,
# add a sibling elevenlabs.local.txt with `voice_id=<your_id>`.

# Voice stability: lower = more variation across the dynamic range Hicks needs
# (weary baseline ↔ ignited peaks). Keep it lower than typical conversational
# voices so the contrast lands.
stability=0.35

# Similarity boost: higher keeps Brian's character intact across the [aggression]
# / [annoyance] tag shifts where stability dips even further.
similarity_boost=0.7
