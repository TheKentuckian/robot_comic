# ElevenLabs TTS configuration for the Rodney Dangerfield profile.
# Format: key=value (one per line; comments start with #).

# Voice name: must match a voice in your ElevenLabs catalog. Dangerfield's
# delivery is nasal, slightly reedy, and world-weary — a voice that sounds like
# it has been through things. Closest prebuilt options:
#   Harry  — mid-range American male with gravitas (default placeholder)
#   Sam    — warm, slightly hoarse, conversational
#   Adam   — firm, resonant (too much confidence — use only if others unavailable)
voice=Harry

# No IVC/PVC clone committed here. If you train a Rodney clone, add a sibling
# elevenlabs.local.txt (gitignored) with:
#   voice_id=<your_ivc_or_pvc_voice_id>
# That file overrides this one at runtime.

# Voice stability: low — Rodney's delivery wavers between the weary baseline
# and sudden mock-outrage peaks. Too much stability flattens the persona.
stability=0.35

# Similarity boost: moderate-high to keep the chosen voice character intact
# even when stability dips during the rapid-fire collapse sections.
similarity_boost=0.68
