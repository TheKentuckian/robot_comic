# ElevenLabs TTS configuration for the Dave Chappelle profile.
# Format: key=value (one per line; comments start with #).

# Voice name: must match a voice in your ElevenLabs catalog. Chappelle's voice
# sits in a smooth mid-range — unhurried baseline with sudden burst surges into
# a higher intensity. Closest prebuilt options:
#   Eric   — warm, intentional pacing, mid-range    (default — deliberate cadence
#             mirrors Chappelle's trademark slow build before the snap)
#   Sam    — mid-range with edge, slightly grittier  (good for the surges, but
#             less warmth on the reflective material)
#
# Chosen: Eric — the unhurried warmth is a better match for Chappelle's layered
# delivery; Eric's pacing supports the deliberate setup that makes the surges land.
voice=Eric

# No IVC/PVC clone committed here. If you train a Chappelle clone, add a sibling
# elevenlabs.local.txt (gitignored) with:
#   voice_id=<your_ivc_or_pvc_voice_id>
# That file overrides this one at runtime.

# Voice stability: LOW-MID — Chappelle alternates between a composed baseline
# and sudden charged surges. Enough stability to keep the cool, low enough that
# the burst moments have room to break through.
stability=0.35

# Similarity boost: high to keep Eric's voice character intact through the
# delivery shifts, so each surge still sounds like the same person.
similarity_boost=0.72
