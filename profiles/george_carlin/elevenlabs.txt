# ElevenLabs TTS configuration for the George Carlin profile.
# Format: key=value (one per line; comments start with #).

# Voice name: must match a voice in your ElevenLabs catalog. Carlin's voice
# was a precise mid-range baritone — measured, not booming. The prebuilt
# catalog options in descending order of fit:
#   Daniel  — warm, authoritative, unhurried        (best default)
#   Charlie — natural, conversational, mid-range
#   Brian   — deep, resonant (slightly too warm/soft for Carlin's edge)
voice=Daniel

# No PVC/IVC clone committed here. If you train one, add a sibling
# elevenlabs.local.txt (gitignored) with `voice_id=<your_id>`.
# The Rickles IVC workflow (scripts/elevenlabs_pvc_clone.py) applies equally.
# voice_id=

# Voice stability: Carlin's delivery is controlled and precise — not the wide
# dynamic swings of Hicks or Rickles. Slightly higher stability than those two,
# but low enough to carry the measured acceleration into rants.
stability=0.45

# Similarity boost: keeps the voice character intact through the delivery-tag
# shifts ([annoyance], [aggression]) that Carlin's dissection sequences use.
similarity_boost=0.7
