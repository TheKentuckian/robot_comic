# ElevenLabs TTS configuration for the Don Rickles profile
# Format: key=value (one per line, comments start with #)

# Voice name: informational when voice_id is set; otherwise must be a named voice
# from the prebuilt catalog (Harry is the placeholder until PVC clone is trained).
voice=Harry

# Custom voice ID for a Professional Voice Clone (PVC).
# To create (from repo root):
#   python scripts/elevenlabs_pvc_clone.py create --profile don_rickles \
#       "Don Rickles" buythistape1993.wav candidate_standup_0015.wav
# Then:
#   python scripts/elevenlabs_pvc_clone.py train <voice_id>
#   python scripts/elevenlabs_pvc_clone.py status <voice_id> \
#       --profile don_rickles --write-config   # auto-fills the voice_id below
# Once trained, voice_id= will be set automatically (or paste it manually).
# voice_id=

# Voice stability: lower = more variation/personality, higher = more consistent.
# Rickles' delivery has big dynamic range — keep this lowish.
stability=0.4

# Similarity boost: how closely to match training data tone.
similarity_boost=0.65
