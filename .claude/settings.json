{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import os,sys,subprocess; p='.claude/hooks/check_worktree_cwd.py'; sys.exit(subprocess.call([sys.executable, p]) if os.path.exists(p) else 0)\""
          }
        ]
      }
    ]
  }
}
